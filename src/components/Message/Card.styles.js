import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 8,
    backgroundColor: "#FFFF",
    borderWidth: 1,
    borderRadius: 5,
    borderColor: "#aeabab",
    marginVertical: 16,
  },
});
